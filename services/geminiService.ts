// AI features have been removed from this application.
// This file is kept as a placeholder to prevent build errors if referenced,
// but it should no longer be used.

export const getFinancialInsights = async (): Promise<string> => {
  return "AI features are disabled.";
};

export const generateAppIcon = async (): Promise<string | null> => {
  return null;
};